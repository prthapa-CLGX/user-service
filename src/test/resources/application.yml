spring:
  application:
    name: user-service
  liquibase:
    enabled: true
    drop-first: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    default-schema: public
  datasource:
    url: jdbc:postgresql://localhost:7654/user_db
    username: robot
    password: robotpass

server:
  port : 9998